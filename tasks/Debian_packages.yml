---
- name: uninstall nginx and dependencies
  apt:
    name: "{{ (lookup('vars', ansible_distribution)).packages }}"
    state: absent
  when: uninstall_nginx is defined

- name: install nginx and dependencies
  apt:
    name: "{{ (lookup('vars', ansible_distribution)).packages }}"
    state: present
    update_cache: yes

